# 🧪 جلسة اختبار شاملة — موظف على موبايل أندرويد
> التاريخ: يونيو 2026 · المختبِر: Claude (مستقل) · viewport: 412×915 (Android)
> الحساب: ناتاليا (موظف، سوريا) PIN 1990

## ✅ الأخطاء المكتشفة والإصلاحات

| # | الشاشة | الخطأ | السبب الجذري | الإصلاح | الحالة |
|---|--------|-------|--------------|---------|--------|
| 1 | عند الدخول (كل التطبيق) | `activity_logs` RLS يمنع الـ insert → سجل نشاط الموظفين معطّل بالكامل | الموظف على PIN session → `auth.uid()=null`، وسياسة INSERT تتطلب auth.uid() (+ نقص GRANT INSERT) | سياسة INSERT متساهلة + GRANT INSERT للـ anon/authenticated (`supabase/activity_logs_rls_fix.sql` — مطبّق على prod) | ✅ |
| 2 | مساحة العمل (`/workspace`) | React: "Each child should have a unique key prop" في TeamPresenceBar | `key={user.userId}` مع userId مكرر/مفقود | `key={user.userId ?? user.userName ?? idx}` | ✅ |
| 3 | الرئيسية (`/`) | تحية المساء المكتوبة "أمسك سعيد" (تعني «امسك»!) — typo | خطأ إملائي في HomeScreen | → "مساؤك سعيد" | ✅ |

## ✅ شاشات اختُبرت بنجاح (بدون أخطاء / بدون انهيار) — 18 شاشة
الرئيسية · الحضور · المهام · الفريق · الإجازات · جدول المناوبات · السلف · تقييم الأداء · الطلبات · KPI/الأداء · التدريب · المحادثات · الزيارات الميدانية · الملفات · مساحة العمل · الملف الشخصي · الإشعارات · الإنجازات · الإعلانات

## 📌 ملاحظات (تحسينات مستقبلية — ليست أخطاء حرجة)
- **تضارب تسمية الفِرق:** يوجد فريقان منفصلان "تيم سوريا" و"سوريا" — يُفضّل توحيدهما (وسيم/فادي على "تيم سوريا"، المندوبون على "سوريا").
- تحذيرات React Router v7 (future flags) — إشعارات إصدار غير مؤثرة، يمكن كتم لاحقاً.
- التحقق بالكاميرا/الوجه لم يُختبر فعلياً (بيئة الاختبار بلا كاميرا) — لكن منطق الـ flow سليم + timeout أُضيف سابقاً.

## الخلاصة
3 أخطاء مكتشفة ومُصلحة · 18 شاشة سليمة · البناء نظيف · جاهز للنشر.
