-- =============================================================
-- تفعيل realtime على shift_groups — 23 آب 2026
--
-- لوحة «مجموعات الدوام» بشاشة الحضور تشترك بتغييرات هذا الجدول
-- حتى تتحدّث لحظة تعديل المسؤول للشركاء، بلا إعادة تحميل الصفحة.
--
-- إضافة فقط · آمن لإعادة التشغيل · لا يمسّ أي بيانات.
-- (بدونه تبقى اللوحة تعمل، لكنها تتحدّث عند فتح الشاشة فقط.)
-- =============================================================
DO $$
BEGIN
  ALTER PUBLICATION supabase_realtime ADD TABLE public.shift_groups;
EXCEPTION
  WHEN duplicate_object THEN NULL;  -- مضاف مسبقاً
  WHEN undefined_object THEN NULL;  -- لا توجد publication بهذا الاسم
END $$;
