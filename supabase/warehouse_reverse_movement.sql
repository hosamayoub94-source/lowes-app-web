-- ================================================================
-- المخازن — عمود التراجع عن الحركات (تصحيح الإدخال الخاطئ)
-- مطبّق حيّاً عبر Supabase SQL Editor (14 يونيو 2026).
-- يربط حركة «تراجع» بالحركة الأصلية → يمنع التراجع المزدوج ويُظهر الحالة.
-- ================================================================
ALTER TABLE wh_movements ADD COLUMN IF NOT EXISTS reverses_id uuid;
-- (wh_movements لها GRANT ALL لـ anon/authenticated على مستوى الجدول؛
--  العمود الجديد مشمول تلقائياً — لا حاجة لمنح إضافي.)
