# تدقيق شامل — LOWE'S App (2026-07-02)
> جولة أدمن كاملة على 44 مساراً + فحص عميق للقاعدة الحيّة (قراءة فقط، صفر تعديل بيانات). التطبيق عليه ~30–37 مستخدماً نشطاً.

## 🧭 المنهجية
- دخول كأدمن (`hosam ayoub`) عبر حقن جلسة يدوية على dev server المتّصل بقاعدة الإنتاج.
- تنقّل آلي على كل المسارات مع اعتراض `console.error` / `window.error` / `unhandledrejection` لكل شاشة.
- فحص القاعدة عبر REST بمفتاحين (anon مقابل service_role) لكشف فجوات RLS — بلا أي كتابة على بيانات حقيقية.

---

## 🔴 أخطاء حرجة (تؤثر على مستخدمين حاليين)

### ✅ C1 — [حُلّ 2026-07-03] أقفال RLS تُفرِغ 3 شاشات لكل مستخدمي الـPIN (جذر واحد)
> **تحديث:** تحقّق REST حيّ (3/7) أظهر أن anon صار يرى نفس أعداد service_role: `accounting_channels` 14/14 · `employee_requests` 1/1 · `activity_logs` 849/849 (كانت 0 لدى anon). الإصلاح نافذ على الإنتاج — لا DDL متبقٍّ لهذا البند. التفاصيل أدناه للأرشيف.

معظم الموظفين يدخلون بجلسة **يدوية (anon، `auth.uid()=NULL`)**، وهذه الجداول سياساتها تشترط `auth.uid()` → تظهر **فارغة تماماً داخل التطبيق** رغم امتلائها في القاعدة:

| الجدول | anon يرى | service يرى | الأثر داخل التطبيق |
|---|---|---|---|
| `accounting_channels` | **0** | 14 | منتقي القناة فارغ في نموذج المحاسبة · `/admin/channels` فارغة · تقرير ربح/خسارة القنوات بلا أسماء |
| `employee_requests` | **0** | 1+ | شاشة «الطلبات/الإجازات/السلف» فارغة · **محرك الرواتب لا يرى السلف المعتمدة فلا يخصمها** |
| `activity_logs` | **0** | 844 | لوحة التدقيق (AuditDashboard) فارغة رغم تسجيل 844 حدثاً |

**الإصلاح جاهز:** `supabase/rls_manual_session_fix_20260702.sql` (يوسّع SELECT إلى `USING(true)` — نفس نموذج بقية النظام؛ آمن/idempotent؛ لا يمسّ بيانات). **يُطبّق من Supabase SQL Editor** (لا يمكن تطبيق DDL عبر REST). هذا هو نفس الجذر الذي أُصلح سابقاً لـ`notifications` و`activity_logs`(insert).

### C2 — 12 من 37 موظفاً نشطاً بلا صفّ راتب
`employee_salary_settings` ناقص لـ12 موظفاً → «تشغيل الدورة» يعطيهم **راتب 0** بلا تنبيه بارز. يحتاج المالك ملء رواتبهم من «الإعدادات → رواتب الموظفين».

---

## 🟠 مشاكل متوسطة

### M1 — بطء استعلام الطلبات (2–3.5 ثانية)
`orders` فيه 32,322 صفاً؛ استعلام الشاشة (`select … limit 500`) يأخذ ~2.1s و`count` ~3.5s. **الحل:** فهارس على `(market, created_at desc)` و`(status)` و`(handler_name)` + استبدال `count=exact` بتقدير أو عدّاد مخزّن. (فهرس واحد مركّب يقصّ الزمن لأقل من 300ms.)

### M2 — تحذير React «unique key» في `/analytics`
`ExecutiveDashboard` يرسم قائمة بلا `key` فريد → تحذير console (غير كاسر، لكن يشير لإعادة رسم غير فعّالة). يُصلح بإضافة `key` مستقر.

### M3 — 4 طلبات نشطة بمزامنة فاشلة (`sync_status=failed`)
عالقة لم تصل جدول جوجل. بانر «أعد المزامنة» موجود بالشاشة — تحتاج ضغطة، أو أتمتة إعادة محاولة مجدولة.

---

## 🟢 ملاحظات وبنية

- **صفر أخطاء console كاسرة** عبر كل الـ44 مساراً (عدا تحذير M2). البنية مستقرة والإقلاع نظيف.
- `/fulfillment` مسار غير مُعرّف (يعرض «الصفحة غير موجودة») — التجهيز يُفتح عبر زر داخلي بشاشة الطلبات لا كمسار مستقل. تنظيف بسيط (إزالة أي رابط قديم له).
- `holidays` / `meta_ad_insights` جدولان غير موجودين بالقاعدة (404) — الميزات تعمل بمصادر أخرى؛ تنظيف مراجع اختياري.
- **نموذج الأمان الفعلي:** كل النظام `USING(true)` + التحقق بطبقة التطبيق (PIN + صلاحيات). الجداول الثلاثة في C1 شذّت عن هذا النموذج بالخطأ.

---

## 🚀 خارطة التطوير — نحو «نظام يعمل بدون تدخّل الموظف»

### المرحلة 0 — إصلاحات فورية (هذا الأسبوع)
1. تطبيق `rls_manual_session_fix_20260702.sql` (يعيد 3 شاشات).
2. ملء رواتب الـ12 موظفاً + تنبيه بارز في الرواتب لأي موظف بلا صفّ راتب.
3. فهرسة `orders` (أداء).

### المرحلة 1 — متانة البيانات (المصدر الواحد)
- **توحيد المبيعات:** `daily_reports` مقابل جداول أخرى — مصدر واحد للحقيقة.
- **توحيد الحضور:** `attendance` (4348 صف) هو الحيّ، بينما محرك الرواتب يقرأ `attendance_records` (صفّان فقط) → اربط المحرك بالجدول الصحيح.
- **`orders.seller_id` (UUID)** بدل مطابقة الاسم النصي الهشّة (`handler_name`).

### المرحلة 2 — الأتمتة (تقليل التدخّل البشري)
- **مزامنة تلقائية لإعادة المحاولة** للطلبات الفاشلة (بدل ضغط الزر يدوياً).
- **دورة رواتب مجدولة** (اقتراح آلي آخر الشهر) بدل الإنشاء اليدوي.
- **ربط ميتا التلقائي** (insights → إنفاق/ROAS) بدل الإدخال اليدوي — الهيكل منشور وينتظر الأسرار فقط.
- **تنبيهات ذكية:** مخزون منخفض، طلب عالق، عميل لم يُتابَع، تارجت قارب.

### المرحلة 3 — تجربة عالمية
- **PWA/موبايل:** استكمال تجاوب كل الشاشات (بدأنا بشاشة الطلبات) + إيماءات + وضع أوفلاين.
- **لوحة تحكّم تنفيذية موحّدة** (KPIs حيّة عبر الفرق) مع تصدير.
- **بحث شامل** (Command Palette موجود — توسيعه ليشمل الطلبات/العملاء/التقارير).
- **مراقبة أخطاء إنتاجية** (Sentry أو ما يعادله) لالتقاط أخطاء المستخدمين تلقائياً.

---

## المرفقات
- SQL الإصلاح: `supabase/rls_manual_session_fix_20260702.sql`
- كل الفحوص أُجريت قراءةً فقط — لم تُنشأ/تُعدّل/تُحذف أي بيانات إنتاج.
