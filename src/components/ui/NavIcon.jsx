// =============================================================
// NavIcon — يعرض أيقونة عنصر قائمة (إيموجي عادةً)، مع استثناء علم
// سوريا: يُستبدَل بصورة SVG ثابتة (SyriaFlag) بدل الاعتماد على خط
// الإيموجي بالنظام — أندرويد بلا تحديث GMS حديث يعرض العلم القديم.
// =============================================================
import SyriaFlag from './SyriaFlag';

export default function NavIcon({ icon }) {
  if (icon === '🇸🇾') return <SyriaFlag />;
  return icon;
}
